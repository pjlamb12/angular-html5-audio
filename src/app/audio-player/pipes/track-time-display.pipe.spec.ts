import { TrackTimeDisplayPipe } from './track-time-display.pipe';

describe('TrackTimeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackTimeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
